import { StyleSheet } from "react-native";

const biographyStyles = StyleSheet.create({
  text: {
    fontFamily: "Inter-SemiBold",
    fontSize: 14,
    lineHeight: 20,
    flexWrap: "wrap",
    flexShrink: 1,
  },
});

export default biographyStyles;
