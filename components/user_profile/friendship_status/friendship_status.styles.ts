import { StyleSheet } from "react-native";

const friendshipStatusStyles = StyleSheet.create({
  incomingContainer: {
    padding: 20,
    gap: 10,
    borderRadius: 16,
    alignItems: "center",
  },
  buttonGroup: {
    width: "100%",
    alignItems: "center",
    justifyContent: "space-around",
    backgroundColor: "transparent",
  },
});

export default friendshipStatusStyles;
