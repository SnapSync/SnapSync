import { StyleSheet } from "react-native";

const userProfileAnimatedHeader = StyleSheet.create({
  imgContainer: {
    alignItems: "center",
    overflow: "hidden",
  },
});

export default userProfileAnimatedHeader;
