import { StyleSheet } from "react-native";

const userProfileTopHeaderStyles = StyleSheet.create({
  viewContainer: {
    height: 52,
    flexDirection: "row",
    paddingVertical: 6,
    gap: 5,
    alignItems: "center",
  },
});

export default userProfileTopHeaderStyles;
